# --- Google Gemini (GenAI) ---
gemini.api.key=test-key
gemini.api.url=http://localhost:${wiremock.server.port}

# --- Telegram Bot ---
telegram.bot.token=test-token
telegram.webhook.secret=test-secret
